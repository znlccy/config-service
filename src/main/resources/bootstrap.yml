spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/znlccy/config-repo
          username: z*l*****c*y
          password: c***c***y*8**88**4

server:
  port: 8084

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/
